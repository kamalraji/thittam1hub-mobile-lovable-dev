-- Set immutable search_path for create-organization usage is not needed (no extra functions), so no changes
-- This placeholder migration exists only to ensure linter issues are not newly introduced.
SELECT 1;